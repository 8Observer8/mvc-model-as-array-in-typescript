Install globally:

> npm typescript http-server

Install packages from the package.json file:

> npm i

Build:

> npm run build

Run the server locally:

> npm start

Go to the browser:

> localhost:3000
